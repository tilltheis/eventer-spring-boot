version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: eventer
      POSTGRES_DB: eventer
      POSTGRES_PASSWORD: eventer
    ports:
      - "5432:5432"